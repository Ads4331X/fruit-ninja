# TODO - Progressive Game Speed (scaled by score)

- [x] Create TODO.md tracking file
- [x] Add `getDifficultyMultiplier()` helper (1x -> 2.5x as score grows)
- [x] Add `updateSpawnRate()` helper (1000ms -> 350ms as score grows)
- [x] Scale upward/sideways/angular velocity in `spawnFruit()` by multiplier
- [x] Scale fruit-half separation speed by multiplier
- [x] Dynamically update spawn timer delay as score changes
- [x] Fix all `tsc` build errors (unused imports/vars + read-only delay)
- [x] Verify with TypeScript type check and production build
      </content>

